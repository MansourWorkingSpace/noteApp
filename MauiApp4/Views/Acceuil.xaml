<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp4.Views.Acceuil"
             Title="Acceuil">
    <VerticalStackLayout Padding="10" Spacing="12">
        <!-- Header -->
        <Frame Padding="8" BackgroundColor="Transparent">
            <Label Text="📱 Acceuil" FontAttributes="Bold" FontSize="18" />
        </Frame>

        <!-- Main card -->
        <Frame Padding="12" >
            <VerticalStackLayout Spacing="10">
                <Label Text="👋 Bonjour !" FontSize="18" />
                <Label Text="Voici vos notes :" />

                <CollectionView ItemsSource="{Binding Notes}" SelectionMode="None" HeightRequest="300">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Padding="8" Margin="4" BorderColor="#ddd">
                                <HorizontalStackLayout Spacing="10">
                                    <Label Text="{Binding Title}" FontSize="16" VerticalOptions="Center" />
                                    <Label Text="{Binding CreatedAt}" FontSize="12" HorizontalOptions="EndAndExpand" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Button Text="[➕ Nouvelle note]" Command="{Binding Router.NavigateToNoteCommand}" />
                <Button Text="[👤 Mon profil]" Command="{Binding Router.NavigateToProfilCommand}" />
            </VerticalStackLayout>
        </Frame>
    </VerticalStackLayout>
</ContentPage>